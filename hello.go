package main

import (
	"fmt"
)

func main() {
	// a := []int{5, 4, 3, 2, 1}
	// a = append(a, 13)
	// fmt.Println(a)

	// vertices := make(map[string]int)

	// vertices["triangle"] = 2
	// vertices["square"] = 3
	// vertices["dodecagon"] = 12

	// delete(vertices, "square")

	// fmt.Println(vertices)

	for i := 0; i < 5; i++ {
		fmt.Println(i)
	}
}
